# All features in one document

# COP

The CopperOS package manager.

## How does it work? 

COP works on the same base as the CopperOS search command. To view an html text it has to request and it download it if we oversimplify it. On that knowledge it downloads the binary from the repo which is a github repo and pastes it right into /bin to simplify work because i don't want to make an unzipper or stuff like JSON files